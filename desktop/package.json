{
  "name": "atomfortune",
  "version": "0.0.6",
  "private": true,
  "description": "AtomFortune — Personal Asset Manager",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "electron .",
    "predist": "cd ../web && npm run build && cp -r .next/static .next/standalone/web/.next/static && cp -r public .next/standalone/web/public && cd ../api && npm run build",
    "dist": "npm run build && { electron-builder --mac; ec=$?; cd ../api && npm rebuild; exit $ec; }"
  },
  "dependencies": {
    "electron-updater": "^6.1.0",
    "atomfortune-api": "file:../api"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^31.0.0",
    "electron-builder": "^24.0.0",
    "typescript": "^5.4.0"
  }
}
