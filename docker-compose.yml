services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: atomfortune
      POSTGRES_USER: atomfortune
      POSTGRES_PASSWORD: atomfortune
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  api:
    build: ./api
    environment:
      DATABASE_URL: postgres://atomfortune:atomfortune@db:5432/atomfortune
      BASE_CURRENCY: TWD
      SNAPSHOT_SCHEDULE: "0 22 * * *"
      EXCHANGERATE_API_KEY: ${EXCHANGERATE_API_KEY}
    ports:
      - "8000:8000"
    depends_on: [db]
  web:
    build: ./web
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000/api/v1
      API_BASE_URL: http://api:8000/api/v1
    ports:
      - "3000:3000"
    depends_on: [api]
volumes:
  pgdata:
